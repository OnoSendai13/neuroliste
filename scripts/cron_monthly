# Monthly cron job for RPPS data update
# Install with: crontab cron_monthly

# Run on 6th of each month at 2:00 AM (after data.gouv.fr updates)
0 2 6 * * cd /path/to/rpps-neuro-app && /bin/bash scripts/monthly_update.sh >> logs/monthly_update.log 2>&1